[package]
name = "clawpit-scene"
version.workspace = true
edition.workspace = true
description = "clawpit 场景模型与线协议（hub 生产，TUI/Web 消费）"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
